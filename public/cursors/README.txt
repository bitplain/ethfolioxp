Add Windows XP cursor files here to enhance the UI.
Suggested filenames:
- xp-arrow.cur (default cursor)
- xp-hand.cur (link cursor)

Optional PNG fallbacks:
- xp-arrow.png
- xp-hand.png

If files are missing, the app falls back to inline SVG cursors.
